{
  "name": "violations-framework",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspace packages/violations-rules && npm run build --workspace packages/violations-cli",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": { "node": ">=18" }
}
